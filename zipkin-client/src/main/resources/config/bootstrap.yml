spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      username: configServer
      password: 9999
      name: zipkin-server #zipkin-server-work.yml
      profile: work
      label: master
      fail-fast: true